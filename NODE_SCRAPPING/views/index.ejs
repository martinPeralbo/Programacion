<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scraping Results</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f0f0f0; /* Fondo color gris claro */
        }
        .results {
            display: grid;
            grid-template-columns: repeat(5, 1fr); /* 5 columnas */
            gap: 20px;
        }
        .result {
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: #4a4a4a; /* Fondo gris oscuro */
            padding: 10px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            color: white; /* Texto blanco para mejor legibilidad */
        }
        .cover {
            max-width: 100%;
            margin-bottom: 10px;
        }
        .name {
            font-weight: bold;
            font-size: 1.2em;
            margin-bottom: 10px;
            text-align: center;
        }
        .link {
            color: #00ff00; /* Enlace en verde claro */
            text-decoration: none;
            margin-bottom: 10px;
        }
        .price {
            margin-bottom: 5px;
        }
        .price.actual {
            color: white; /* Precio actual por defecto en blanco */
        }
        .price.actual.lower {
            color: green; /* Precio actual menor al mínimo en verde */
        }
        .price.actual.higher {
            color: red; /* Precio actual mayor al máximo en rojo */
        }
        .last-seen {
            font-size: 0.8em;
            color: #ccc;
        }
    </style>
</head>
<body>
    <h1>Scraping Results</h1>
    <div class="results">
        <% results.forEach(result => { %>
            <div class="result">
                <img class="cover" src="<%= result.imageUrl %>" alt="<%= result.name %> cover">
                <div class="name"><%= result.name %></div>
                <a class="link" href="<%= result.url %>" target="_blank">Ver juego</a>
                <div class="price actual <%= result.priceClass %>">
                    Actual: <%= result.price %>€
                </div>
                <div class="price">Mínimo: <%= result.minPrice %>€</div>
                <div class="price">Máximo: <%= result.maxPrice %>€</div>
                <div class="last-seen">Última actualización: <%= new Date(result.lastSeen).toLocaleString() %></div>
            </div>
        <% }) %>
    </div>
</body>
</html>